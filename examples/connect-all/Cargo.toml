[package]
name = "connect-all"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
eventful-rs = { path = "../../eventful-rs", default-features = false }

[dev-dependencies]
assert_cmd = "2"
