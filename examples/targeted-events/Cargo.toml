[package]
name = "targeted-events"
edition = "2024"
publish = false
rust-version.workspace = true
license.workspace = true

[dependencies]
eventful-rs = { path = "../../eventful-rs", default-features = false }

[dev-dependencies]
assert_cmd = "2"
